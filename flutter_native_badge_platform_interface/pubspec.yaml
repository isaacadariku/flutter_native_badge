name: flutter_native_badge_platform_interface
description: A common platform interface for the flutter_native_badge plugin.
repository: https://github.com/isaacadariku/flutter_native_badge/tree/main/flutter_native_badge_platform_interface
issue_tracker: https://github.com/isaacadariku/flutter_native_badge/issues
version: 2.0.1

topics:
  - badge
  - notification
  - app-icon

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

scripts: derry.yaml
