name: flutter_native_badge_foundation_example
description: Demonstrates how to use the flutter_native_badge_foundation plugin.
publish_to: "none"

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_native_badge_foundation:
    path: ../
  flutter_native_badge_platform_interface:
    path: ../../flutter_native_badge_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
